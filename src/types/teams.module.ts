export interface RemoteTeam {
  icon: string;
  org: string;
  slug: string;
  team_name: string;
}

export interface Team {
  university: string;
  slug: string;
  name: string;
  imageUrl: string;
}
