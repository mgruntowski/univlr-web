export interface RemoteState {
  id: number;
  nome: string;
  icone: string;
  regiao: string;
  sigla: string;
}

export interface State {
  id: number;
  name: string;
  icon: string;
  region: string;
  abbreviation: string;
}
